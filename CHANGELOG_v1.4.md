# SIAP ARU v1.4

- Fixed sidebar active state: only the most-specific route is highlighted.
- Root Admin centralized as superuser.
- Added per-format `sequence_start`.
- Added optional custom base number when creating outgoing letters.
- Backdate/custom collision becomes `.1`, `.2`, etc.
- Number series remain independent per document format/type.
- Outgoing list displays numeric sequence separately from document type.
- Added operational-user bootstrap/reset SQL.
