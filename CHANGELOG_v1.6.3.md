# Changelog v1.6.3

- Fix `Periode tidak valid.` saat upload laporan keuangan bulanan.
- Fix parser periode ketika `period_month` berasal dari MySQL sebagai JavaScript `Date`.
- Fix validasi periode pada attachment adjustment dan reversal.
- Database/schema tidak berubah.
